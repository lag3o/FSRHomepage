<%= form_for(@rolle) do |f| %>
  <% if @rolle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rolle.errors.count, "error") %> prohibited this rolle from being saved:</h2>

      <ul>
      <% @rolle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
